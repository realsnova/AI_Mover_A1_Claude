# 開發接手指南（單字冒險 A1 Movers）

> 給下一個開發 session 的 Claude（或任何 AI 開發工具）。閱讀本文件後即可接續開發，不需重新探索。

## 1. 專案一句話

台灣國小三年級學生的 A1 Movers 單字學習遊戲：單一 HTML 檔、寶可夢收服/進化 + DQ 式冒險、四種題型、道館考試、純點擊 UI、全繁中介面。

## 2. 目前進度

| Phase | 內容 | 狀態 |
|-------|------|------|
| 0 | 395 字官方單字資料（wordlist.json） | ✅ 完成，官方 A–Z 表 100% 覆蓋驗證 |
| 1 | MVP：地圖/學習模式/看圖+聽音題/收服/圖鑑/存檔/重置 | ✅ 完成 |
| 2 | 拼字題/填空題/道館(計時/徽章)/金幣商店/8-bit音效 | ✅ 完成 |
| — | 瀏覽器實機測試 34 項 | ✅ 全過（驗證清單.md 有完整紀錄） |
| 3 | SRS 間隔重複/進化/每日獎勵 | ✅ 完成，驗證清單 S/E/D/R 全過（Phase3_設計與驗證清單.md 有紀錄） |
| 4 | 全量 395 字、10 區域、劇情、儀表板 | ✅ 完成，驗證清單 W/U/T/N/B/P/R 全過（Phase4_設計與驗證清單.md 有紀錄） |
| 5 | 出廠品管、強固化、家長說明 | ⬜ 待做 → Phase5_設計與驗證清單.md |

**下一步：執行 Phase 5。**每個 Phase 的文件內含設計細節、開發步驟、AI Prompt、驗證清單；實作完必須依清單瀏覽器實測通過才能交付（使用者明確要求）。

Phase 3 已加入：`Game.SRS`（today/daysBetween/stat/grade/due/buildQuiz）、進化查表工具、每日獎勵；state 新增 lastLogin/streak/shinyCaught/shinyPending，wordStats 擴充 box/lastSeen/correctDays，caught 擴充 evoStage。

Phase 4 已加入（單一檔案，內嵌全量資料後約 129KB）：
- **資料內嵌**：`WORDS_ALL`（395 字）、`WORD_EMOJI`/`WORD_ZH` 查表（`EMOJI_BANK`/`ZH_BANK` 已改為其別名）、`MONSTERS`（10 區）、`ALL_MONSTERS`（攤平 77 隻）。
- **區域引擎**：`wordsOf/monstersOf/bossOf/chaptersOf/gymNeed` 全域函式；`Game.regionUnlocked/caughtInRegion/regionName/masteredCount`。`startBattle(regionKey, chapterIdx?)`、`startGym()` 皆 per-region；caught 新增 `region` 欄位。
- **冠軍之路(mixed)**：125 字分 3 章（chaptersOf），道館需三章各熟 `MIXED_CH_NEED=30` 字（超夢）。
- **劇情**：`playOpening/typeStoryLine/storyClick/storySkipAll/endOpening`（首次遊玩，state.storySeen）；`playEnding/finishEnding`（集滿 10 徽章→結局+自由挑戰 freeChallenge）。
- **儀表板**：頂欄「📊 進度」→ `showDash/masteryPct/exportRecord/showExportFallback`。
- **存檔**：`save()` 寫 version:4；`load()` 解析失敗 → 備份 `SAVE_KEY_corrupt` 後重置。`GYM_BOSS` 常數已移除（改 bossOf）。
- 已移除的舊全域：`WORDS`(16字)、`REGION_MONSTERS`、`EMOJI_BANK/ZH_BANK` 物件字面、`GYM_BOSS`。

## 3. 檔案清單（outputs 資料夾）

| 檔案 | 用途 |
|------|------|
| `單字冒險.html` | **主程式**（含 Phase 1~4，約 1750 行 / 129KB，已內嵌全量 395 字 + 10 區 77 隻） |
| `wordlist.json` | 395 字完整資料（meta + words），已內嵌進主程式（Phase 4） |
| `wordlist_檢查用.csv` | 給使用者人工檢查的表格版 |
| `monsters.json` | Phase 4 素材：10 區 77 隻寶可夢（含進化鏈+中文名）+ 10 館主，已驗證無重複 |
| `Phase3/4/5_設計與驗證清單.md` | 各階段設計與必測清單 |
| `驗證清單.md` | Phase 1+2 的 34 項實測紀錄（測試方法參考範本） |
| `A1-Movers-互動學習網頁-任務規劃.md` | 最初整體規劃 |
| `wl_01~10*.json` | wordlist 的分主題原始檔（保留備查，可不理會） |

## 4. 關鍵技術決策（不要走回頭路）

1. **Twemoji CDN**：cdnjs 的 twemoji 是 404！正確引用：
   - script：`https://cdn.jsdelivr.net/npm/@twemoji/api@15.1.0/dist/twemoji.min.js`
   - 圖檔 base：`https://cdn.jsdelivr.net/gh/jdecked/twemoji@15.1.0/assets/`（folder: svg, ext: .svg）
2. **emoji 政策**：資料中只放 Windows 10 可顯示的 emoji（Unicode ≤12）；無合適 emoji 的字留空字串 → 遊戲以白底中文字卡顯示（zh-fallback）。Twemoji 是加值層，離線時退回原生 emoji 也不能破版
3. **寶可夢 sprite**：`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/{id}.png`（shiny 加 /shiny/）；img onerror 全域備援顯示 👾
4. **音效**：全部 Web Audio 程式合成（tone/sfxCorrect/sfxWrong/sfxCoin/jingleVictory），無外部音檔、無版權疑慮
5. **發音**：SpeechSynthesis en-US rate 0.85；Chrome 需使用者點擊過頁面後才允許（實際使用必然滿足）
6. **單一檔案原則**：所有 CSS/JS/資料內嵌 `單字冒險.html`，使用者雙擊即玩（file:// 環境，fetch 不可用）
7. **版權**：寶可夢素材僅限個人/課堂非商業使用，不得公開部署（規劃文件第 0 章）

## 5. 開發環境已知問題與解法（重要，會踩坑）

1. **outputs 掛載同步會卡住舊版**：`單字冒險.html` 被多次 Edit 後，Linux 沙盒掛載可能一直讀到舊內容。
   - 驗證實際檔案一律用 Read/Grep 工具（Windows 側，永遠是最新）
   - 需要在沙盒處理最新內容時：用 Write 另存新檔名（新檔案會同步），用完刪除
2. **瀏覽器擴充不能開 file:// 網址**。實測方法（已驗證可行）：
   - navigate 到 `https://example.com` → javascript_tool 建立 `<input type="file">` + FileReader 存 window.__HTML → `file_upload` 工具上傳 單字冒險.html → `document.open(); document.write(window.__HTML); document.close()`
   - 注入後 localStorage/語音/CDN 都正常，可完整測試
3. **截圖座標 ≠ 視窗座標**：截圖可能縮放（如 1568 寬 vs 實際 1920）。點擊一律用 read_page 的 ref，不要用截圖座標
4. **javascript_tool 45 秒超時**：長流程（自動答題迴圈）要拆成多次呼叫，每次 ≤ 3 題；超時不代表頁面壞了，下一次呼叫讀狀態即可
5. **speechSynthesis 測試**：先用 computer 工具真實點擊頁面一次，再測 utterance 的 onstart/onend 事件；直接腳本呼叫會得到 not-allowed（是政策不是 bug）
6. **document.write 注入後 DOMContentLoaded 可能不觸發**：主程式的 initTwemoji 已改為 readyState 判斷，任何載入方式都會初始化（勿改回）

## 6. 標準開發循環（每個 Phase 照做）

1. 讀對應的 `PhaseN_設計與驗證清單.md`
2. 用 Edit 修改 `單字冒險.html`
3. 新程式區塊抽出到 /tmp 做 `node --check`（完整檔案檢查用「另存新檔名→沙盒 node --check」）
4. 依第 5 節方法把檔案注入 Chrome，逐項執行該 Phase 驗證清單（真實點擊+截圖+JS 斷言）
5. 發現問題 → 修正 → 重新注入重測
6. 全過後：更新驗證清單 .md 紀錄結果 → present_files 交付 → 告知使用者測試重點

## 7. 快速接手指令（給使用者貼）

> 「請閱讀 開發接手指南.md，接續執行 Phase 3（依 Phase3_設計與驗證清單.md），完成並通過瀏覽器實測後交付。」
